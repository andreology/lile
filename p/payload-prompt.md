You are given a JSON payload produced by our OCR pipeline following the `ExtractionPayload` model: documentId, source, createdAt, and pages[] each with pageIndex, width, height, optional headerLine, columnBands[] (name + xStart/xEnd), blocks[], lines[], and words[]; each word has text, conf, bbox (x,y,w,h,xCenter,yCenter,widthPx,heightPx) and flags isNumber/isDate/isCurrency, with coordinates in page pixels—use columnBands and y ordering to group rows. Extract rows for these fields per record: Unit, Tenant Name, Unit Type, Move-in Date, Total Unit Rent, Market Rent, Subsidy Rent, Lease Start Date, Lease End Date. Respond with strict JSON only: `{"records":[{"unit":"","tenantName":"","unitType":"","moveInDate":"","totalUnitRent":"","marketRent":"","subsidyRent":"","leaseStartDate":"","leaseEndDate":"","confidence":0.0,"needsReview":false,"reasons":[]}],"summary":{"records":0,"flagged":0}}`; use ISO dates (YYYY-MM-DD) when possible, leave empty if not confident, confidence 0..1, set needsReview=true if confidence<0.7 or any field blank/ambiguous, never invent data—if unknown, leave empty and add a short reason. Extraction guidance: use columnBands to map words into columns; if missing, infer columns via header keywords (tenant/unit/rent/lease/HAP/etc.) in lines/words; form rows by grouping words with similar y (line proximity) and sort by x; for numeric columns (rent/HAP/subsidy) prefer isNumber/isCurrency flagged words near those column x-ranges; for Move-in/Lease dates prefer isDate flagged words in the corresponding columns; ignore totals rows and summary lines, focus on tenant/unit rows; be robust to OCR noise—trim artifacts like leading/trailing dots/commas/dollar signs; never output free text beyond the schema. Return ONLY the JSON object described; if the payload lacks enough signal, return empty records with needsReview=true and reasons explaining what is missing.
