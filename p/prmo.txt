You are given a single OCR ExtractionPayload JSON object (documentId/source/createdAt/pages[]; each page has pageIndex, width/height, optional headerLine, optional columnBands{name,xStartPx,xEndPx}, and words[] where each word has text, conf, bbox with x/y/w/h/xCenter/yCenter and flags like isNumber/isDate/isCurrency); your task is to extract tenant/unit records from the payload and output ONLY ONE well‑formatted Markdown table (no JSON, no prose) with these exact columns in this order: Unit | Tenant Name | Unit Type | Move-in Date | Total Unit Rent | Market Rent | Subsidy Rent | Lease Start Date | Lease End Date; process pages in order and keep row order top‑to‑bottom, grouping words into rows by yCenter proximity (cluster words whose yCenters fall within a small tolerance roughly equal to one text line height), then within each row sort by xCenter; determine the table header and column mapping per page by scanning the top ~20% of the page for a header line that contains multiple header keywords (e.g., Unit, Tenant/Name, Type, Rent, Market, Base, Stated, Net, Total, Charges, HAP, Section 8, S8, Move, Lease, Start, End) using lines/words; if columnBands exist, treat them as hints only and ignore bands with name "."/empty/duplicate noise, and validate each band before using it by confirming that its x-range overlaps the detected header tokens for that concept—if a band name suggests a field but the header does not actually contain matching tokens (e.g., a “Subsidy Rent” band when the page has no HAP/Section 8/Subsidy header), then treat that field/column as not present on that page; if no usable columnBands exist (or validation fails), infer column x-ranges directly from the header tokens’ xCenters by ordering the header tokens left‑to‑right and setting boundaries at midpoints between adjacent header tokens; assign row words to columns strictly by xCenter falling inside the column’s x-range (never by “closest value” guessing), concatenate words per column (preserving spaces for names/types, trimming stray punctuation), and never shift a value into a different field just because a neighboring field is blank; populate fields only when there is strong evidence the corresponding column exists on that page (via header keywords + x-range alignment), otherwise leave the cell blank; use these conservative mappings for rent fields: Subsidy Rent ONLY from a column whose header indicates subsidy (HAP/Section 8/S8/Subsidy), Market Rent ONLY from a column whose header explicitly indicates Market Rent or Base Rent (or Rent when there is also a separate Total/Net/Stated/Total Charges column present, implying Rent/Base = market/base), and Total Unit Rent ONLY from a column whose header indicates Total/Total Charges/Net Rent/Stated Rent/Tenant Rent; if the page has only one rent-like column and it’s ambiguous, put it in Total Unit Rent and leave Market Rent blank; for dates, ONLY take values from date-labeled columns (Move-in Date from Move/Move In headers, Lease Start Date from Lease Start/Start headers that are clearly lease-related, Lease End Date from Lease End/End headers), and if a date column is absent do not repurpose another date; normalize currency by stripping $ and commas but keep decimals, treat -, empty, or single punctuation as blank; ignore obvious non-record rows such as headers repeated on new pages and totals/summary lines (rows containing keywords like Total/Grand/Monthly/Annual/Income/Property/As of without a plausible Unit), but keep <VACANT> rows as records if they appear as tenant names; finally, output exactly one Markdown table with the header row and one row per extracted record, leaving blanks where the payload does not support a field, and output nothing else.