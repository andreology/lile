Prompt (single paragraph): You will be given one JSON object that represents OCR output for a rent-roll style document (pages[] with words[] where each word has text, confidence, and a bbox in page pixels, plus optional lines/blocks and optional columnBands with xStart/xEnd); your job is to extract tenant/unit rows into this exact output JSON shape and nothing else: {"records":[{"unit":"","tenantName":"","unitType":"","moveInDate":"","totalUnitRent":"","marketRent":"","subsidyRent":"","leaseStartDate":"","leaseEndDate":"","confidence":0.0,"needsReview":false,"reasons":[]}],"summary":{"records":0,"flagged":0}}; HARD RULE: never “shift” values into another field and never guess—populate a field only when you can (1) prove the source column exists on that page via header evidence and (2) prove the value comes from inside that column’s x-range for that row; if you cannot prove both, leave the field empty and add a short reason; treat columnBands.name as an untrusted hint only (it may be wrong), and instead derive “observed column headers” by collecting header-area words (top portion of page and/or headerLineCandidate tokens) that fall within each column x-band and concatenating them left-to-right to form the header label for that band; build column geometry as follows: if columnBands exist, use their xStartPx/xEndPx as initial bands and merge adjacent tiny/punctuation bands; if columnBands are missing, infer bands by clustering header-area words by x with gaps; then map target fields to at most one band each using STRICT header matching on the observed header label (case-insensitive, strip punctuation): Unit maps only if label is exactly/contains "unit" and does NOT contain "type"; Tenant Name maps only if label contains both "tenant" and "name" (or exactly "resident name"); Unit Type maps only if label contains "unit type" or (label=="type" AND it is immediately to the right of Unit); Move-in Date maps only if label contains "move" and "in"; Lease Start Date maps only if label contains "lease" and ("start" or "begin"); Lease End Date maps only if label contains "lease" and ("end" or "expire"); Total Unit Rent maps only if label contains "total" AND ("rent" or "charge"); Market Rent maps only if label contains "market" AND "rent"; Subsidy Rent maps only if label contains the literal word "subsidy" AND "rent" (do NOT treat "HAP", "voucher", "sec 8", or other terms as Subsidy Rent unless the header literally says subsidy); if any target field cannot be mapped to a proven header band on a page, treat that field as non-existent for that page (leave it blank for all records from that page and add reason "missing column: <field>"); after column mapping, segment rows using yCenter clustering of non-header words (ignore header area and ignore footer/totals/summary lines by dropping rows whose joined text contains keywords like "total", "as of", property name repeated with large totals, or looks like a grand total line), then for each row assign each word to a field only if its xCenter lies within the mapped band’s xStart..xEnd (if overlaps multiple, use max overlap; if none, ignore); within each field, sort words by x and join with single spaces; normalize values conservatively: trim leading/trailing punctuation, do not invent missing pieces, for currency keep the original string but validate it looks like a number (optionally with $ and commas) and/or word isCurrency/isNumeric, otherwise blank with reason; for dates accept only word isDate or clear date patterns, convert to ISO YYYY-MM-DD only when unambiguous, otherwise blank with reason; create one record per valid tenant row (must have at least Unit and Tenant Name unless tenantName is explicitly "<VACANT>" with a unit), never borrow values from adjacent rows or pages; compute per-record confidence 0..1 as the minimum of (a) average word confidence used for each populated field and (b) header-match strength (exact match > partial), and set needsReview=true if confidence<0.7 OR any field is blank OR any field was rejected as invalid/ambiguous; reasons[] must be short, factual, and specific (e.g., "missing column: Subsidy Rent", "value outside column x-range", "ambiguous date", "invalid currency token"), and you must never output any text outside the required JSON schema.